<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>dev.chronit</groupId>
  <artifactId>chronit-parent</artifactId>
  <version>1.0.0-SNAPSHOT</version>
  <packaging>pom</packaging>

  <name>chronit</name>
  <description>Scheduled Minecraft Java Edition server check-in bot</description>

  <modules>
    <module>chronit-core</module>
    <module>chronit-driver-mcpl</module>
    <module>chronit-web</module>
    <module>chronit-via</module>
    <module>chronit-app</module>
  </modules>

  <properties>
    <maven.compiler.release>21</maven.compiler.release>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>

    <!--
      MCProtocolLib publishes only SNAPSHOTs. Pinned to a specific timestamped build so the
      image is reproducible; bump both properties together when moving to a newer Minecraft
      version. mcpl.version is what Maven resolves, mcpl.snapshot documents the exact build.
    -->
    <mcpl.version>26.2-SNAPSHOT</mcpl.version>
    <mcpl.snapshot>26.2-20260809.160751-16</mcpl.snapshot>
    <minecraft.version>26.2</minecraft.version>
    <minecraft.protocol>776</minecraft.protocol>

    <minecraftauth.version>5.0.2</minecraftauth.version>
    <viaversion.version>5.11.0</viaversion.version>
    <vialoader.version>3.0.4</vialoader.version>
    <jackson.version>2.22.1</jackson.version>
    <picocli.version>4.7.7</picocli.version>
    <cronutils.version>9.2.1</cronutils.version>
    <slf4j.version>2.0.16</slf4j.version>
    <logback.version>1.6.2</logback.version>
    <junit.version>6.1.3</junit.version>
  </properties>

  <repositories>
    <repository>
      <id>opencollab-snapshots</id>
      <url>https://repo.opencollab.dev/maven-snapshots/</url>
      <releases><enabled>false</enabled></releases>
      <snapshots><enabled>true</enabled></snapshots>
    </repository>
    <repository>
      <id>opencollab-main</id>
      <url>https://repo.opencollab.dev/main/</url>
    </repository>
    <repository>
      <id>viaversion</id>
      <url>https://repo.viaversion.com</url>
    </repository>
  </repositories>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>dev.chronit</groupId>
        <artifactId>chronit-core</artifactId>
        <version>${project.version}</version>
      </dependency>
      <dependency>
        <groupId>dev.chronit</groupId>
        <artifactId>chronit-driver-mcpl</artifactId>
        <version>${project.version}</version>
      </dependency>
      <dependency>
        <groupId>dev.chronit</groupId>
        <artifactId>chronit-web</artifactId>
        <version>${project.version}</version>
      </dependency>
      <dependency>
        <groupId>dev.chronit</groupId>
        <artifactId>chronit-via</artifactId>
        <version>${project.version}</version>
      </dependency>

      <dependency>
        <groupId>org.geysermc.mcprotocollib</groupId>
        <artifactId>protocol</artifactId>
        <version>${mcpl.version}</version>
      </dependency>
      <dependency>
        <groupId>net.raphimc</groupId>
        <artifactId>MinecraftAuth</artifactId>
        <version>${minecraftauth.version}</version>
      </dependency>

      <dependency>
        <groupId>com.fasterxml.jackson.core</groupId>
        <artifactId>jackson-databind</artifactId>
        <version>${jackson.version}</version>
      </dependency>
      <dependency>
        <groupId>com.fasterxml.jackson.dataformat</groupId>
        <artifactId>jackson-dataformat-yaml</artifactId>
        <version>${jackson.version}</version>
      </dependency>
      <dependency>
        <groupId>com.fasterxml.jackson.datatype</groupId>
        <artifactId>jackson-datatype-jsr310</artifactId>
        <version>${jackson.version}</version>
      </dependency>

      <dependency>
        <groupId>com.cronutils</groupId>
        <artifactId>cron-utils</artifactId>
        <version>${cronutils.version}</version>
        <exclusions>
          <!-- Pulls in an old EL implementation only used for message templating. -->
          <exclusion>
            <groupId>org.glassfish</groupId>
            <artifactId>jakarta.el</artifactId>
          </exclusion>
        </exclusions>
      </dependency>
      <dependency>
        <groupId>info.picocli</groupId>
        <artifactId>picocli</artifactId>
        <version>${picocli.version}</version>
      </dependency>
      <dependency>
        <groupId>org.slf4j</groupId>
        <artifactId>slf4j-api</artifactId>
        <version>${slf4j.version}</version>
      </dependency>
      <dependency>
        <groupId>ch.qos.logback</groupId>
        <artifactId>logback-classic</artifactId>
        <version>${logback.version}</version>
      </dependency>

      <dependency>
        <groupId>org.junit</groupId>
        <artifactId>junit-bom</artifactId>
        <version>${junit.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <dependencies>
    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <pluginManagement>
      <plugins>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-compiler-plugin</artifactId>
          <version>3.13.0</version>
          <configuration>
            <compilerArgs>
              <!-- -try is off because holding a lock through a try-with-resources lease, without
                   referencing the resource in the body, is deliberate and correct here. -->
              <arg>-Xlint:all,-serial,-processing,-try</arg>
              <arg>-parameters</arg>
            </compilerArgs>
          </configuration>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-surefire-plugin</artifactId>
          <version>3.5.2</version>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-shade-plugin</artifactId>
          <version>3.6.2</version>
        </plugin>
      </plugins>
    </pluginManagement>
  </build>
</project>
